# Test Environment Configuration
NODE_ENV=test

# Database (use SQLite for testing)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=x_bookmarker_test
DB_USER=test_user
DB_PASSWORD=test_password

# Redis (mock for testing)
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT
JWT_SECRET=test_jwt_secret_key_for_testing_only
JWT_EXPIRES_IN=7d

# X API (mock values for testing)
X_CLIENT_ID=test_client_id
X_CLIENT_SECRET=test_client_secret
X_CALLBACK_URL=http://localhost:3000/auth/x/callback

# AI Services (optional for testing)
OPENAI_API_KEY=test_openai_key
ANTHROPIC_API_KEY=test_anthropic_key